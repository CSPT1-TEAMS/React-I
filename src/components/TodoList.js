import React, { Component } from 'react';
should render an input tag
should store todos in state
should render a Todo component for every todo in state'
should add a new Todo when `addTodo` is called
export default List;